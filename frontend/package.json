{
  "name": "gj-camp-frontend",
  "version": "0.1.1",
  "private": true,
  "proxy": "http://localhost:5000",
  "dependencies": {
    "@paypal/react-paypal-js": "^8.9.2",
    "axios": "^1.6.0",
    "chart.js": "^4.5.1",
    "emoji-picker-react": "^4.16.1",
    "react": "^18.2.0",
    "react-chartjs-2": "^5.3.1",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.16.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "ESLINT_NO_DEV_ERRORS=true CI=false node generate-build-info.js && node update-sw-version.js && react-scripts build",
    "prebuild": "node generate-build-info.js && node update-sw-version.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
  ,
  "engines": {
    "node": ">=18.0.0"
  }
}
